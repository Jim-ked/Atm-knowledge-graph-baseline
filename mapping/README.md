# 人工映射

`字段映射.xlsx` 是当前项目唯一人工语义映射入口，固定为三张表：实体映射、属性映射、关系映射。

本体刷新时：
- 已填写的人工映射不得被覆盖；
- 本体新增类/属性/关系只新增对应的 `[待映射]` 行；
- 本体删除或改名导致已有映射失效时，应显式报错并由人工修正；
- 默认一个本体属性只接受一个权威源字段，不做隐式跨源覆盖或融合。

普通字段、类、属性或关系调整通过当前本体与本工作簿处理，不要求在 Java Core 中增加航空字段判断。
