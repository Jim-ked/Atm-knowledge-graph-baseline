# 查询资产

Java Core 只提供领域无关查询能力；需要固定语义的查询定义放在这里，不把具体航空业务语义硬编码进 QueryService。

已确定的查询能力范围：
- 实体查询；
- 完整一跳；
- 完整 K 跳；
- 两实体路径；
- 固定语义路径/命名查询；
- 后续相关实体、路线结构、影响查询可作为命名查询资产实现。

完整 K 跳返回约束范围内的全部节点，以及这些返回节点之间原图中存在的全部关系。不得通过随机抽样、top-N 或静默 LIMIT 改变查询语义；超限时应显式反馈。
